mutation updateSchoolarYear(
  $id_year: Int!
  $rector: String!
  $secretary: String!
  $comment: String
) {
  updateScholarYear(id_year: $id_year, rector: $rector, secretary: $secretary, comment: $comment) {
    id_year
  }
}
